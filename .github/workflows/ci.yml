name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          # Full history: the sitemap's <lastmod> is the page's last commit
          # date (apps/web/scripts/lastmod.mjs), and the crawler suite checks
          # the built sitemap against it. A shallow clone leaves both empty,
          # which passes without proving anything.
          fetch-depth: 0

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "22"
          cache: true

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Install dependencies
        run: vp install --frozen-lockfile

      # Playwright is a dependency of apps/web, so its binary is linked there
      # rather than at the workspace root.
      - name: Install Playwright browsers
        run: vp exec playwright install --with-deps chromium
        working-directory: apps/web

      - name: Run lint
        run: just lint-ci

      # A static check of .claude-plugin/*.json and the skill; needs no auth.
      - name: Validate Claude plugin
        run: |
          npm install -g @anthropic-ai/claude-code
          just validate-plugin

      - name: Run tests
        run: just test

      - name: Build
        run: just build
